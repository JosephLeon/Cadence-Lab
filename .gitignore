.venv/
__pycache__/
*.pyc
.python-version

# Python build / packaging artifacts
*.egg-info/
build/
dist/

# Test / lint / type caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Project outputs and intermediates
output/
files/
*.analysis.json
*.mic.16k.wav

# Streamlit — keep project-wide config.toml, ignore per-user credentials
.streamlit/credentials.toml

# Frontend (Vite + React)
app/node_modules/
app/dist/
app/.vite/
app/*.log

# Tauri (Rust shell)
app/src-tauri/target/
app/src-tauri/gen/schemas/

# Secrets — load via python-dotenv from .env (template is .env.example)
.env
.env.local

# Models cached by faster-whisper / huggingface (if pointed at the repo)
.cache/

# OS / editor noise
.DS_Store
.vscode/
.idea/
